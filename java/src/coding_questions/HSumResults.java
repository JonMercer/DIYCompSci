package coding_questions;

/**
 * Created by Odin on 2017-03-07.
 */
public interface HSumResults {


    //find if a sum of two elements in numbers equals target
    //throw illegal argument if numbers is less than one
    //assume input is not sorted and can be negative
    boolean targetInArray(int[] numbers, int target);
}
